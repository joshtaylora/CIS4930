import { SelectPostDirective } from './select-post.directive';

describe('SelectPostDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectPostDirective();
    expect(directive).toBeTruthy();
  });
});
