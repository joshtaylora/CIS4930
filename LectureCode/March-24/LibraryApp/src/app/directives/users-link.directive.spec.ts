import { UsersLinkDirective } from './users-link.directive';

describe('UsersLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new UsersLinkDirective();
    expect(directive).toBeTruthy();
  });
});
