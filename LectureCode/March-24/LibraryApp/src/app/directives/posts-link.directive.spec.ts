import { PostsLinkDirective } from './posts-link.directive';

describe('PostsLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new PostsLinkDirective();
    expect(directive).toBeTruthy();
  });
});
